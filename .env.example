# Supabase — online multiplayer, shared by every game in the hub.
# Copy to `.env` and fill in from your Supabase project settings.
# Only Realtime broadcast on public channels is used, so the anon key is enough.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
