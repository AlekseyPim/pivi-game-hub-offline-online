/**
 * Web stub for the banner — no ads on web, and this keeps the native-only
 * `react-native-google-mobile-ads` module out of the web bundle graph.
 */
export function AdBanner(_props: { enabled?: boolean }) {
  return null;
}
